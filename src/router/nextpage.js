import React from "react";
import { Auth } from "../component/auth";
const Nextpage = (ID) => {
    console.log("next", ID)
    console.log(ID)
    return(
        <div> name:
        </div>
    )
    


}

export default Nextpage;